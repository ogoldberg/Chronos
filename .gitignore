# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ is intentionally NOT ignored — see DEPLOYMENT.md.
# Railway builds were OOM-killing during 'vite build', so we ship the
# pre-built bundle in the repo and let Railway just `npm start` it.
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/worktrees/
